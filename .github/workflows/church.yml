name: blank

on:
  push:

jobs:
  ratio:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pump: ["distribute", "consumption", "intervene", "oppress", "definite"]
        invest: ["budget", "rely", "accuracy", "separate"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar QE
